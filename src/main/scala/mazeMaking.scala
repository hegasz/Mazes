package Buildin

object mazeMaking{

    // design optimal data structure to then be able to apply
    // Kruskal's algorithm.
    // write kruskals and disjoint set stuff for kruskal's in 
    // the algorithms folder.

    type grid = Array[Array[Double]]
}